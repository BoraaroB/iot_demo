# Copy to .env and adjust for local development. Never commit .env.
# All values below are placeholders — do not reuse them outside local dev.

# ---- Postgres (vehicle_db, alert_db) ----
POSTGRES_USER=iiot
POSTGRES_PASSWORD=changeme_dev_only
POSTGRES_PORT=5432

# ---- TimescaleDB (telemetry_db) ----
TIMESCALEDB_USER=iiot
TIMESCALEDB_PASSWORD=changeme_dev_only
TIMESCALEDB_PORT=5434

# ---- Redis ----
REDIS_PORT=6380

# ---- EMQX ----
EMQX_DASHBOARD_PASSWORD=changeme_dev_only
EMQX_MQTT_PORT=1883
EMQX_DASHBOARD_PORT=18083

# ---- Kafka ----
KAFKA_PORT=9092

# ---- Kafka UI ----
KAFKA_UI_PORT=8080
