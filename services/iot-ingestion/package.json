{
  "name": "iot-ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MQTT to Kafka bridge: subscribes to factory/vehicle topics and publishes validated events to Kafka.",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@iiot/config": "*",
    "@iiot/events": "*",
    "@iiot/logger": "*",
    "@iiot/types": "*",
    "express": "^5.2.1",
    "kafkajs": "^2.2.4",
    "mqtt": "^5.16.0",
    "pino-http": "^11.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
