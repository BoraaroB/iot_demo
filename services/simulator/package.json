{
  "name": "simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Simulates factory vehicles publishing telemetry/status over MQTT.",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@iiot/config": "*",
    "@iiot/events": "*",
    "@iiot/logger": "*",
    "@iiot/types": "*",
    "express": "^5.2.1",
    "mqtt": "^5.16.0",
    "pino-http": "^11.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
