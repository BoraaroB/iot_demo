{
  "name": "realtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kafka consumer that fans out events to subscribed clients over WebSocket.",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@iiot/config": "*",
    "@iiot/events": "*",
    "@iiot/logger": "*",
    "@iiot/types": "*",
    "express": "^5.2.1",
    "pino-http": "^11.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
